@model IMS.ViewModels.CounterpartyViewModel

@{
    ViewData["Title"] = $"Редагувати: {Model.OriginalName}";
    Layout = "_Layout";
}

<h1>@ViewData["Title"]</h1>
<hr />
<div class="row">
    <div class="col-md-6">
        <form asp-action="Edit" asp-route-name="@Model.OriginalName" method="post">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <input type="hidden" asp-for="OriginalName" />

            <div class="mb-3">
                <label asp-for="Name" class="form-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
                <div class="form-text text-warning">Увага: Ви редагуєте первинний ключ (Назву). Переконайтеся, що нова назва унікальна.</div>
            </div>
            <div class="mb-3">
                <label asp-for="PhoneNumber" class="form-label"></label>
                <input asp-for="PhoneNumber" class="form-control" />
                <span asp-validation-for="PhoneNumber" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Email" class="form-label"></label>
                <input asp-for="Email" type="email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label class="form-label">Призначені Ролі</label>
                <div class="border p-2 rounded">
                    @if (Model.RolesCheckboxes != null && Model.RolesCheckboxes.Any())
                    {
                        @for (int i = 0; i < Model.RolesCheckboxes.Count; i++)
                        {
                            <div class="form-check">
                                <input type="hidden" asp-for="@Model.RolesCheckboxes[i].Id" />
                                <input type="hidden" asp-for="@Model.RolesCheckboxes[i].Name" />
                                <input class="form-check-input" type="checkbox"
                                       name="SelectedRoleIds" 
                                       value="@Model.RolesCheckboxes[i].Id"
                                       id="role_@Model.RolesCheckboxes[i].Id"
                                       asp-for="@Model.RolesCheckboxes[i].IsAssigned" />
                                <label class="form-check-label" for="role_@Model.RolesCheckboxes[i].Id">
                                    @Model.RolesCheckboxes[i].Name
                                </label>
                            </div>
                        }
                    }
                    else
                    {
                        <p class="text-muted">Немає доступних ролей для призначення.</p>
                    }
                </div>
                <span asp-validation-for="SelectedRoleIds" class="text-danger"></span>
            </div>


            <div class="mb-3 mt-4">
                <button type="submit" class="btn btn-primary">Зберегти зміни</button>
                <a asp-action="Index" class="btn btn-secondary">Назад до списку</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
 }