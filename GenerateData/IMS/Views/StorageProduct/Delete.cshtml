@model IMS.Models.StorageProduct

@{
    ViewData["Title"] = $"Видалити {Model.ProductNameNavigation?.ProductName ?? Model.ProductName} зі складу {Model.StorageName}";
    Layout = "_Layout";
}

<h1>@ViewData["Title"]</h1>

<h3 class="text-danger">Ви впевнені, що хочете видалити цей товар зі складу?</h3>


<p>Ця дія видалить запис про наявність товару на цьому складі.</p>



<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-3">
            Склад
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.StorageName)
        </dd>
        <dt class="col-sm-3">
            Товар
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.ProductNameNavigation.ProductName)
        </dd>
        <dt class="col-sm-3">
            Поточна кількість
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.Count)
        </dd>
        <dt class="col-sm-3">
            Мін. Залишок
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.MinimalCount)
        </dd>
    </dl>

    <form asp-action="Delete" asp-route-storageName="@Model.StorageName" asp-route-productName="@Model.ProductName" method="post">
        @Html.AntiForgeryToken()

        <button type="submit" class="btn btn-danger">Видалити зі складу</button>
        |
        <a asp-controller="Storage" asp-action="Details" asp-route-name="@Model.StorageName" class="btn btn-secondary">Скасувати</a>
    </form>
</div>